# GitHub Import

GitHub import is future M4 work.

For v1, prefer public repository import with minimal permissions and no write scopes. Private repo support is out of scope until explicitly approved.
